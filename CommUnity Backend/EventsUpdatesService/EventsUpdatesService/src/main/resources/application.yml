eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 7002
spring:
  application:
    name: EventsUpdatesService
  datasource:
    url: jdbc:mysql://localhost:3306/eventsupdatesdb
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true